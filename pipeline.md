# Weekly Paper Process — 视频结构与排版设计

## 总览：11 个场景，约 90 秒

```
场景 0:   标题页                     TTS 时长 (~7s)
场景 1:   论文 0 演示卡 (全屏帧)     TTS 时长 (~11s)
场景 2:   论文 1 演示卡 (全屏帧)     TTS 时长 (~11s)
场景 3:   论文 2 演示卡 (全屏帧)     TTS 时长 (~11s)
场景 4:   论文 3 演示卡 (全屏帧)     TTS 时长 (~11s)
场景 5:   论文 4 演示卡 (全屏帧)     TTS 时长 (~11s)
场景 6:   结尾卡                      TTS 时长 (~5s)
```

> 所有场景时长由对应 TTS 音频时长精确决定。
> **去掉了论文标题卡独立页面**（保持画面连贯）。
> **去掉了标题卡到演示卡的过渡黑帧**（TTS 播报期间画面不能黑）。
> 标题页 TTS 播报结束后才切到第一篇论文——如果 TTS 还有余音，画面继续展示标题页直到播完。

---

## 1. 标题页 (TitleCard)

| 元素 | 位置 | 细节 |
|------|------|------|
| 背景 | 全屏 | 纯白 `#ffffff`，叠加淡灰径向渐变装饰 (`#f8fafc` → `#f1f5f9`) |
| 装饰圈 | 右上 + 左下 | 半透明紫色圆形光晕 (`#6366f1` 10-15% 透明度) |
| **主标题** | 居中偏上 | `HuggingFace一周论文速览`，64px，深色 `#1a1a2e`，字重 700 |
| 装饰线 | 标题下方 | 紫色渐变横线，从左到右展开动画 (0→200px) |
| **日期范围** | 装饰线下方 | 如 `2026.7.13 - 2026.7.17`，28px，灰色 `#64748b` |
| **底部 Logo** | **底部居中** (bottom:30) | `PeperPaper`，20px，灰色 `#94a3b8`，字重 400 |
| 入场动画 | 文字 | 淡入 + 上滑 (30px→0)，0-20 帧 |
| 停留 | TTS 全时长 | 静态展示，不退场（直到 TTS 播完自然切到下个 Composition） |
| 退场 | 最后 5 帧 | 淡出到白色 |

> 不设退场淡出，保持显示直到 Composition 自然结束。

---

## 2. 论文演示页 (PaperDemoPage)

| 元素 | 位置 | 细节 |
|------|------|------|
| 背景 | 全屏 | Demo 帧或白色 |
| Demo 帧序列 | 全屏铺满 | 从 `public/frames/paper_{i}/frame_{0001-NNNN}.png` 循环播放，24fps |
| 底部渐变 | 底部 15% | 黑色→半透明渐变，提升标题文字可读性 |
| **分类标签 tag** | **左上角** (top:20, left:24) | 内联标签，圆角 14px，13px，**蓝底白字** `background:#6366f1, color:#ffffff`，不再按领域分色 |
| **论文标题** | **左下角** (bottom:24, left:24) | 22px，字重 700，白色 `#ffffff`，最多 2 行，带文字阴影 |
| **arXiv 链接** | **标题下方** | 14px，灰色 `#94a3b8`，带白色文字阴影，显示 arxiv ID |
| **指标 metrics** | **右下角** (bottom:24, right:24) | **蓝底白字** (`background:#6366f1, color:#ffffff`)，圆角 6px，12px |
| 入场动画 | 所有叠加层 | 6-18 帧淡入（不淡出，停留到结束） |

> **不需要分类标签按领域分色**——统一蓝底白字 `#6366f1`。
> **旁白文字** (narration) 不显示在画面上，只走 TTS 音频。
> **指标过滤**: 排除含 "upvote"/"upvotes" 字样的兜底项，只显示真实 benchmark 指标（如 "MMLU 90%"、"2× faster"）。过滤后无指标**区域留空**，不显示任何指标标签。

---

## 3. 结尾页 (OutroPage)

| 元素 | 位置 | 细节 |
|------|------|------|
| 背景 | 全屏 | 纯白 + 淡灰径向渐变 |
| 装饰圈 | 上部 | 半透明紫色圆形光晕 |
| **主文字** | 居中 | `感谢观看`，36px，深色 `#1a1a2e`，字重 600 |
| **副文字** | 主文字下方 | 换行分割，如 `下期再见` |
| 停留 | TTS 全时长 | 静态展示，不退场 |
| 退场 | 无 | 画面保持到 TTS 的最后一帧，不提前黑帧 |

---

## 4. 关键技术约束

| 约束 | 说明 |
|------|------|
| **TTS 时长 = 场景时长** | 每段 Composition 的 `durationInFrames` 由 TTS 音频精确决定。TTS 播完画面才切 |
| **无过渡黑帧** | 场景与场景之间直接切换，TTS 播报期间画面不能黑 |
| **论文标题卡去掉** | 不单独展示标题页，直接进入演示卡，减少场景切换次数 |
| **标签统一蓝底白字** | tag 和 metrics 都用 `background:#6366f1, color:#ffffff`，不按领域分色 |
| **GitHub 去掉了** | 演示卡只显示 arXiv ID，不显示 GitHub（信息过载） |
| **结尾播完再结束** | OutroCard 不设提前退场动画，TTS 播完才自然结束 |

---

## 5. 技术规格

| 项目 | 值 |
|------|----|
| 分辨率 | **1920×1080** (1080p) |
| 帧率 | 24fps |
| 编码 | H.264 + AAC (视频 8Mbps, 音频 128kbps) |
| 渲染引擎 | **Remotion 4.0** (`@remotion/bundler` + `@remotion/renderer`) |
| 组件 | TypeScript + React 18 |
| 音频 | TTS 通过 `mux_av()` 混入视频轨（`-map 0:v:0 -map 1:a:0`） |
| BGM | `assets/bgm_struggle.mp3`，音量 0.12 |
| TTS | **Qwen3-TTS Elise**（端口 8765），不可用时无硬 fallback（pipeline 报错退出） |

---

## 6. 颜色系统

```
背景:         #ffffff (纯白)
渐变:         #f8fafc → #f1f5f9 (极浅灰)
主文字:       #1a1a2e (深黑蓝)
次要文字:     #64748b (石板灰)
浅色文字:     #94a3b8 (浅灰蓝)
强调色:       #6366f1 → #818cf8 (紫色)
标签底色:     #6366f1 (蓝)
标签文字:     #ffffff (白)
```

## 7. 指标提取优化方向

当前 `extract_metrics` 在摘要提取实际 benchmark 指标的效果不理想。优化方向：

1. 增加中文 benchmark 名称匹配（"准确率 99%"、"top-1 92%"）
2. 从论文标题中直接提取关键能力词（如 "并行感知"、"端到端"、"Subject-Driven"）
3. 如果正则未匹配到有效的 metric，**该论文右下角直接留空**，不显示 upvotes
4. 后续可引入 DeepSeek API 对摘要进行 1 次调用提取关键指标

## 8. 论文介绍文本优化方向

当前 narration 从 `ai_summary` 截取第一句，质量不够。

优化方向：
1. 接入 DeepSeek API，传入标题 + 摘要，生成 100-150 字精彩旁白
2. 旁白风格：[问题/背景] → [团队/机构] → [创新点] → [一句话总结]
3. 写入 `_papers_w{week}.json` 供后续手动微调
4. pipeline 提供 `fetch-only` 模式，只抓取论文 + 生成旁白，不渲染
